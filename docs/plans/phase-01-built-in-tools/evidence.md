# Phase 01 Evidence

## Evidence Log

| Item | Reference | Notes |
| --- | --- | --- |
| Runtime failure observation | `docker logs 5f90d40eaa6a7142b838f478c681d69aa75641d48948abf5ca9971b77d8b2978` | `/v1/responses` completed successfully, the named agent invoked `Microsoft.Extensions.AI.FunctionInvokingChatClient`, and the assistant replied that it had no live web-browsing access instead of invoking a tool. |
| Current agent wiring | `src/Common/LeanKernel.Logic/Extensions/IServiceCollectionExtensions.cs` | Uses `OpenAIClient(...).GetChatClient(...).AsIChatClient()` with `.UseFunctionInvocation()`, which is already the provider-agnostic local function-calling path but currently has no registered tool runtime. |
| Current endpoint mapping | `src/Services/LeanKernel.Gateway/Programs.cs` | Keeps `app.MapOpenAIResponses()` on the current no-argument path as required by repo guidance. |
| Current container provider route | `docker inspect 5f90d40eaa6a7142b838f478c681d69aa75641d48948abf5ca9971b77d8b2978`, `docker-compose.yml` | Gateway is configured with `OPENAI__BASEURL=http://litellm:4000/v1`. |
| Downstream transport confirmation | `docker logs leankernel-litellm` | The failing turn reached LiteLLM as `POST /v1/chat/completions`, confirming the current runtime is not using a Responses-backed agent path. |
| Available LiteLLM tool alias | `config/litellm/config.yaml` | LiteLLM already defines a dedicated `tool` route alias, which is the concrete target for tool-capable agent turns instead of the current `medium` default. |
| Recent wiki-availability failure | `docker logs 5f90d40eaa6a7142b838f478c681d69aa75641d48948abf5ca9971b77d8b2978` | For “How many wiki documents do you have?”, the assistant answered that it could only see a wiki if connected through a tool, confirming the absence of callable knowledge tools in the current runtime and motivating either downstream list/count capability validation or bounded local aggregation over returned result sets. |
| Current memory-only GBrain path | `src/Common/LeanKernel.Logic/Providers/MemoryProvider.cs`, `src/Services/LeanKernel.Gateway/Providers/GBrainMemoryClient.cs` | GBrain is currently used only through `IMemoryClient` for background search/persistence and AI-context injection, not as a callable tool surface. |
| Current GBrain callable operations status | Current worktree evidence | The current worktree directly confirms callable GBrain use for memory `search` and `put_page`; read/list-style wiki parity with the older repo still needs explicit validation against the current GBrain deployment before it is treated as guaranteed. |
| Older built-in tool model | `~/source/repos/leankernel/src/LeanKernel.Tools/ToolsServiceCollectionExtensions.cs`, `~/source/repos/leankernel/src/LeanKernel.Tools/ToolRegistry.cs` | The older repo defined built-in LeanKernel tools in a shared registry and did not depend on provider-hosted web/file tools. |
| Older `web_search` behavior | `~/source/repos/leankernel/src/LeanKernel.Tools/BuiltIn/Internet/WebSearchTool.cs` | `web_search` is a LeanKernel-owned tool contract using Brave (`BRAVE_API_KEY`) with DuckDuckGo fallback, created via `IServiceScopeFactory` and resolving its `HttpClient` from a per-invocation scope — not a provider-hosted OpenAI tool. Grounds Appendix B web-search config and Appendix C scoping. |
| Older `file_search` behavior | `~/source/repos/leankernel/src/LeanKernel.Tools/BuiltIn/FileSystem/FileSearchTool.cs` | `file_search` is a LeanKernel-owned local search bounded to an allowed root via `FileSystemSupport.ResolveWithinRoot`, with bounded depth/scan/byte limits and name/content matching. Grounds the `Files.RootPath` confinement gate. |
| Older deterministic data-tool inspiration | `~/source/repos/leankernel/src/LeanKernel.Tools/BuiltIn/Data/JsonTransformTool.cs`, `DatabaseQueryTool.cs`, `CsvXlsxReadWriteTool.cs` | The former repo already treated deterministic data-shaping work as local tool responsibility. This informs the Phase 01 addition of smaller bounded `calculate` / aggregation helpers without copying the older surface area wholesale. |
| Older `SKILL.md` schema + loader | `~/source/repos/leankernel/src/LeanKernel.Plugins/BuiltIn/Skills/SkillParser.cs`, `SkillDefinition.cs`, `DynamicSkillTool.cs`, `SkillExtensions.cs` | YAML frontmatter with `runtime`/`auth`/`egress`/`operations`; tool name `{skill}_{operation.id}`; HTTP path placeholders + flags→query/body; egress allowlist with loopback/private/redirect blocking; secrets from `/run/secrets/<ref>` or `SKILL__<REF>`. Grounds Appendix A and the safety gates. |
| Older governance + registry | `~/source/repos/leankernel/src/LeanKernel.Tools/ToolRegistry.cs`, `ToolGovernancePolicy.cs`, `~/source/repos/leankernel/src/LeanKernel.Abstractions/Interfaces/IToolRegistry.cs` | `AllowedToolNames` takes precedence, `AllowedCategories` applied otherwise; shared name-keyed registry with `AddTools`. Grounds Appendix B allowlist semantics. |
| Older MAF tool adaptation path | `~/source/repos/leankernel/src/LeanKernel.Agents/Orchestration/ToolDefinitionAIToolAdapter.cs`, `~/source/repos/leankernel/src/LeanKernel.Agents/TurnPipeline.cs` | `ToolDefinition` adapted to an `AIFunction` (JSON schema built from `ToolParameter`s) and passed as provider-agnostic `ChatOptions.Tools`. Confirms the wiring target for step 5. |
| Older GBrain knowledge service + MCP ops | `~/source/repos/leankernel/src/LeanKernel.Knowledge/GBrainKnowledgeService.cs`, `~/source/repos/leankernel/src/LeanKernel.Abstractions/Interfaces/IKnowledgeService.cs` | `IKnowledgeService` exposes `SearchAsync`/`GetPageAsync`/`PutPageAsync`/`DeletePageAsync` over MCP tools `search`/`get_page`/`put_page`/`delete_page`. Grounds Appendix D capability pre-check and the separation from `IMemoryClient`. |
| MAF local tooling primitives | `~/.nuget/packages/microsoft.extensions.ai/10.6.0/lib/net10.0/Microsoft.Extensions.AI.xml`, `~/.nuget/packages/microsoft.agents.ai/1.13.0/lib/net10.0/Microsoft.Agents.AI.xml` | The installed package set supports local tool execution through `ChatOptions.Tools`, `AIFunction`, and function invocation. Framework skill providers may help internally, but the phase target remains first-class LeanKernel-owned tools. |
| Hosted tool guidance used for disqualification | Microsoft Learn: web search, file search, code interpreter, providers overview | The documented MAF/OpenAI `web_search`, `file_search`, and `code_interpreter` features are provider-hosted and therefore do not satisfy the provider-agnostic LiteLLM objective by themselves. |
